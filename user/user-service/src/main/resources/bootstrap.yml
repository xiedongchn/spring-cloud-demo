#bootstrap文件优于application文件加载
spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: cloud-config-server-9091
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9090/eureka
