#bootstrap文件优于application文件加载
spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: user-service-8082
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9099/eureka
