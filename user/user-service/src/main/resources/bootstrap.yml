#bootstrap文件优于application文件加载
spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: cloud-config-server-7200
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7100/eureka
    fetch-registry: true
    registry-fetch-interval-seconds: 8

